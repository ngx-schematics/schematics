{
  "$schema": "http://json-schema.org/schema",
  "id": "HuskyOptions",
  "title": "Husky Schematic Options",
  "type": "object",
  "properties": {
    "package-manager": {
      "type": "string",
      "enum": ["npm", "yarn"],
      "description": "The desired package manager for running scripts.",
      "default": "npm",
      "x-prompt": "Which package manager do you want to use for running scripts?"
    },
    "precommit-lint": {
      "type": "boolean",
      "description": "Add a pre-commit hook for linting",
      "default": true,
      "x-prompt": "Do you want to add a pre-commit lint script?"
    },
    "precommit-format": {
      "type": "boolean",
      "description": "Add a pre-commit hook for formatting",
      "default": false,
      "x-prompt": "Do you want to add a pre-commit format script?"
    },
    "prepush-lint": {
      "type": "boolean",
      "description": "Add a pre-push hook for linting?",
      "default": false,
      "x-prompt": "Do you want to add a pre-push lint script?"
    },
    "postmerge-install": {
      "type": "boolean",
      "description": "Add a post-merge hook for running install.",
      "default": false,
      "x-prompt": "Do you want force an install after a merge?"
    }
  },
  "required": []
}
